# Actual Bridge Configuration
ACTUAL_BRIDGE_URL=http://actual-bridge:3000

# API Key for actual-bridge authentication
# Must match the BRIDGE_API_KEY in actual-bridge
BRIDGE_API_KEY=your_bridge_api_key_here

# Logging
LOG_LEVEL=INFO
