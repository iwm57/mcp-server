# Actual Budget API Configuration
ACTUAL_SERVER_URL=https://actual.luxky.app
ACTUAL_SYNC_ID=your-budget-sync-id
ACTUAL_BUDGET_PASSWORD=your-budget-file-password

# Bridge Configuration
PORT=3000
DATA_DIR=/data

# API Key Authentication (REQUIRED for production)
# Generate a secure random key (e.g., 32+ characters)
# All /mcp routes will require this key in the x-api-key header
BRIDGE_API_KEY=your_bridge_api_key_here
